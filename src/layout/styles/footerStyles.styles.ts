import styled from 'styled-components';
import { colors } from '../../theme/colors';

export const FooterWrapper = styled.div`
  border-top: 1px solid ${colors.secondColor};
  height: 3vh;
  display: flex;
  color: ${colors.secondColor};
  align-items: center;
  padding: 1rem;
`;
